# Running Sum of 1D Array

## Pattern

Prefix sum.

## Mental Trigger

Need cumulative totals from left to right.

## Approach

Keep a running variable and append the updated sum for every element.

## Complexity

Time: `O(n)`

Space: `O(n)` for the result.

## Alternative

This can be done in-place if mutation is allowed.
